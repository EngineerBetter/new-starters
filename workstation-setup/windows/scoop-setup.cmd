rem to setup scoop see https://scoop.sh
rem scoop will install the software locally for your user. Use the -g flag if you want them globally
scoop install^
 curl^
 wget^
 git^
 graphviz^
 1password-cli^

rem scoop install -g firefox
rem scoop install -g vscode
rem scoop install -g zoom
rem scoop install -g slack